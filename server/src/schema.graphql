type Query {
  tours: [Tour!]!
}

type Mutation {
   addComment(tourId: ID!, text: String!): Comment
}

# TODO add pictures
type Tour {
  id: ID!,
  name: String!,
  description: String,
  price: Float!,
  transport: String,
  comments: [Comment!]!
}

# TODO add createdAt and createdBy
type Comment {
  id: ID!,
  text: String!,
  tour: Tour!
}